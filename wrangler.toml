name = "autism-social-coach"
main = "src/index.ts"
compatibility_date = "2025-11-15"

[durable_objects]
bindings = [
  { name = "AGENT", class_name = "SocialSkillsAgent" }
]

# If your environment supports AI binding via wrangler v4, you can add the "ai" section:
[ai]
binding = "AI"
